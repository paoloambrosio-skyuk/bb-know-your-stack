gatling {
  data {
    writers = "console, file, graphite"

    graphite {
      host = "localhost"
    }
  }
  http {
    ahc {
      allowPoolingConnections = false
      keepAlive = false
      connectTimeout = 1000
      requestTimeout = 5000
      readTimeout = 5000
      maxRetry = 0
    }
  }
}